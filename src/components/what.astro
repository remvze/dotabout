<section class="about">
  <div>
    <h2>Your page stays yours.</h2>
    <p>
      Dotabout reads a public file from GitHub and turns it into a clean,
      shareable profile. Edit the file whenever you want. The page follows.
    </p>
  </div>

  <div class="flow">
    <code>github.com/you/.about/about.json</code>
    <span aria-hidden="true">→</span>
    <code>dotabout.me/@you</code>
  </div>
</section>

<style>
.about {
  display: grid;
  grid-template-columns: 0.9fr 1.1fr;
  gap: 72px;
  align-items: center;
  padding: 72px 0;

  & h2 {
    font-size: var(--font-xlg);
    font-weight: 500;
    letter-spacing: -0.025em;
  }

  & p {
    margin-top: 16px;
    line-height: 1.7;
    color: var(--color-foreground-subtle);
  }
}

.flow {
  display: flex;
  flex-direction: column;
  gap: 14px;
  align-items: flex-start;
  padding: 30px;
  background: var(--color-neutral-100);
  border-radius: 16px;

  & code {
    font-family: var(--font-mono);
    font-size: var(--font-xsm);
    color: var(--color-foreground);
  }

  & span {
    margin-left: 12px;
    color: #f06595;
    transform: rotate(90deg);
  }
}

@media (width <= 720px) {
  .about {
    grid-template-columns: 1fr;
    gap: 36px;
  }

  .flow {
    padding: 24px;
    overflow: hidden;
  }
}
</style>
