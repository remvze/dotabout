<section class="setup" id="get-started">
  <div class="heading">
    <h2>Live in three steps.</h2>
    <p>No signup or configuration required.</p>
  </div>

  <ol class="steps">
    <li>
      <span>1</span>
      <h3>Create <code>.about</code></h3>
      <p>Make a public repository in your GitHub account.</p>
    </li>
    <li>
      <span>2</span>
      <h3>Add <code>about.json</code></h3>
      <p>Start with the template and make the content yours.</p>
    </li>
    <li>
      <span>3</span>
      <h3>Open your page</h3>
      <p>Visit <code>dotabout.me/@you</code> and share it anywhere.</p>
    </li>
  </ol>

  <div class="resources">
    <a
      class="primary"
      href="https://github.com/remvze/dotabout/blob/main/template/about.json"
      >Use the template</a
    >
    <a href="https://github.com/remvze/dotabout/blob/main/docs/schema.md"
      >Read the schema <span>→</span></a
    >
  </div>
</section>

<style>
.setup {
  padding: 120px 0 100px;

  & .heading h2 {
    font-size: var(--font-xlg);
    font-weight: 500;
    letter-spacing: -0.025em;
  }

  & .heading p {
    margin-top: 10px;
    color: var(--color-foreground-subtle);
  }
}

.steps {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 42px;
  margin-top: 56px;
  list-style: none;

  & li > span {
    display: block;
    margin-bottom: 20px;
    font-family: var(--font-mono);
    font-size: var(--font-xsm);
    color: #f06595;
  }

  & h3 {
    font-weight: 500;
  }

  & p {
    margin-top: 9px;
    line-height: 1.65;
    color: var(--color-foreground-subtle);
  }

  & code {
    font-family: var(--font-mono);
    font-size: var(--font-xsm);
    color: var(--color-foreground);
  }
}

.resources {
  display: flex;
  gap: 24px;
  align-items: center;
  margin-top: 52px;

  & a {
    font-size: var(--font-sm);
    font-weight: 500;
    color: var(--color-foreground-subtle);
    text-decoration: none;

    &:hover {
      color: var(--color-foreground);
    }
  }

  & .primary {
    padding: 11px 17px;
    color: var(--color-neutral-50);
    background: var(--color-neutral-950);
    border-radius: 10px;

    &:hover {
      color: var(--color-neutral-50);
    }
  }
}

@media (width <= 720px) {
  .setup {
    padding: 90px 0 80px;
  }

  .steps {
    grid-template-columns: 1fr;
    gap: 38px;
  }
}
</style>
